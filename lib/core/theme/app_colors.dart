import 'package:flutter/material.dart';

class AppColors {
  // Primary Accent
  static const primary = Color(
    0xFFFE517B,
  );

  // Backgrounds
  static const scaffoldBackground = Colors.white;

  // Text
  static const textPrimary = Colors.black;
  static const textSecondary = Color(
    0xFF6B6B6B,
  );
  static const white = Colors.white;

  // Borders / Divider
  static const border = Color(
    0xFFE0E0E0,
  );
  static const Color fieldFill = Color(
    0xFFF5F6FA,
  );
  static const Color fieldBorder = Color(
    0xFFE5E7EB,
  );
  static const Color fieldFocusedBorder = primary;
  static const Color background = Color(
    0xFFFFFFFF,
  );
}
